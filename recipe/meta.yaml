{% set name = "jupysql-plugin" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/jupysql_plugin-{{ version }}.tar.gz
  sha256: b9dcfc146cfe51fb2000b6cc769e6b0b8df83ffb28c025a7e75150a41fa7d2f4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.7
    - hatchling >=1.4.0
    - jupyterlab >=3.4.7,<4.0.0
    - hatch-jupyter-builder
    - hatch-nodejs-version
    - pip
  run:
    - python >=3.7
    - ploomber-core
    - jupyter_server

test:
  imports:
    - jupysql_plugin
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/ploomber/jupysql-plugin
  summary: Jupyterlab extension for JupySQL
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - edublancas
    - neelasha23
